#!/usr/bin/env bash
# vault — the VAULT daily-driver TUI (deck + command deck).
SELF="$(readlink -f "${BASH_SOURCE[0]}")"
exec uv run --with textual --with requests --with websockets \
  python3 "$(dirname "$SELF")/vault_top.py" "$@"
